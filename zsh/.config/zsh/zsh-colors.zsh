#!/usr/bin/env zsh

autoload -U colors && colors

fg_directory="%{$fg[black]%}"
bg_directory="%{$bg[white]%}"

fg_git_branch="%{$fg[black]%}"
bg_git_branch="%{$bg[green]%}"

fg_git_status="%{$fg[white]%}"
bg_git_status="%{$bg[red]%}"

fg_venv="%{$fg[black]%}"
bg_venv="%{$bg[magenta]%}"

